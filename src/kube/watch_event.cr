# module Kube
#   class WatchEvent < K8S::Kubernetes::Resource
#     # @return [K8s::Resource] the resource the `object` in the WatchEvent refers to
#     def resource
#       @resource ||= K8S::Kubernetes::Resource.new(object)
#     end
#   end
# end
